# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env.local
.env.*.local
.env.production
.env.development
# but commit the template
!.env.production.example
!.env.example

# vercel
.vercel

# fallow analyzer cache
.fallow/

# claude code
.claude/

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
# Migrations MUST be committed — the production entrypoint runs
# `prisma migrate deploy` which reads migration.sql files from this dir.
# Leaving them out of git means prod deploys apply nothing and schema drifts.
